services:
  pypath-backend:
    build: .
    container_name: pypath-backend
    ports:
      - "8001:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./app:/app/app
    restart: unless-stopped
